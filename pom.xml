<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>net.ME1312.ASM</groupId>
    <artifactId>bridge-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <maven.compiler.plugin.version>3.13.0</maven.compiler.plugin.version>
        <maven.jar.plugin.version>3.4.2</maven.jar.plugin.version>
        <cyclonedx.maven.plugin.version>2.9.3</cyclonedx.maven.plugin.version>
        <bridge.version>${project.version}</bridge.version>
        <build.commit>unknown</build.commit>
        <build.ref>unknown</build.ref>
        <build.number>local</build.number>
    </properties>

    <modules>
        <module>bridge-asm</module>
        <module>bridge-plugin</module>
        <module>bridge</module>
        <module>bridge-test</module>
    </modules>

    <profiles>
        <profile>
            <id>minecraft-it</id>
            <activation>
                <property>
                    <name>minecraft.serverJar</name>
                </property>
            </activation>
            <modules>
                <module>bridge-mc-it</module>
            </modules>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${maven.jar.plugin.version}</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Implementation-Title>${project.artifactId}</Implementation-Title>
                            <Implementation-Version>${project.version}</Implementation-Version>
                            <Implementation-Vendor>SupraCraft</Implementation-Vendor>
                            <Build-Commit>${build.commit}</Build-Commit>
                            <Build-Ref>${build.ref}</Build-Ref>
                            <Build-Number>${build.number}</Build-Number>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.cyclonedx</groupId>
                <artifactId>cyclonedx-maven-plugin</artifactId>
                <version>${cyclonedx.maven.plugin.version}</version>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>make-aggregate-sbom</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>makeAggregateBom</goal>
                        </goals>
                        <configuration>
                            <outputFormat>json</outputFormat>
                            <outputName>bridge-sbom</outputName>
                            <includeBomSerialNumber>true</includeBomSerialNumber>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
